name: HandsOn_Practice
on:
  workflow_dispatch:
    inputs:
      environments:
        default: dev
        description: choose envs
        required: true
        type: choice
        options:
          - dev
          - prod
        

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Run a one-line script
        run: echo Hello, world!
