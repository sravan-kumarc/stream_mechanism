name: Day1 First Workflow

on:
  push:

jobs:
  first-job:
    runs-on: ubuntu-latest

    steps:
      - name: Step 1
        run: echo "Hello Sravan"

      - name: Step 2
        run: node -v

      - name: Step 3
        run: echo "Workflow working fine"