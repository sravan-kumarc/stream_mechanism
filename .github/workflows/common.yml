name: Common Workflow

on:
  workflow_call:

jobs:
  reusable-job:
    runs-on: ubuntu-latest

    steps:
      - name: Step 1
        run: echo "Hello from reusable workflow"

      - name: Step 2
        run: echo "This is shared logic"
